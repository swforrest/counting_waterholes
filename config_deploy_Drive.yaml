#Updated to run on the GPU PC at CDS
#Prepared to run a deployment of model detection on a novel aoi. 

#AF 26.03.2025 

yolo_dir: C:/Users/fossatia/Documents/Waterholes_project/yolov5

python: python

raw_images: D:/Waterholes_project/counting_waterholes/deployment/raw_images #tif files

proj_root: C:/Users/fossatia/Documents/Waterholes_project/counting_waterholes/counting_waterholes

# reccommended to change outdir for each run if using different images/params!!!

#AF: Output of the pre-training processes 
output_dir: D:\Waterholes_project\counting_waterholes\deployment\output 

#AF: creating new paths to manipulate the output of the segmentation. 
segmented_images : D:\Waterholes_project\counting_waterholes\deployment\output\images\train
segmented_labels : D:\Waterholes_project\counting_waterholes\deployment\output\labels\train

# Validation run with Best weights from your last model training or the weights of your choice. CHANGE:
weights: D:\yolo_runs\exp_v4\weights\best.pt
 
cache: False

# yaml config describing the training data and classes
#AF: various values and parameters of the model manipulation, to be checked when running it or change in the code directly. 
workers: 6
BATCH_SIZE: 8
EPOCHS: 100
img_size: 416
img_stride: 104
TILE_SIZE: 416
STRIDE: 104

# training Dataset
path: D:/Waterholes_project/counting_waterholes/deployment
# pngs created, json labels, and location of the created labelme_auto.json files.
pngs: ./pngs
#tif files 
raw_images: ./raw_images
segmented_images: ./segmented_images # Segmented images 
labels: ./labels #Segmented labels  
classifications: ./classifications # Classifications (segmented labels from model detection)
plots: ./plots #figures and plots outputs 

training_path: D:\Waterholes_project\counting_waterholes\deployment\train

train: D:\Waterholes_project\counting_waterholes\deployment\train\train\images
  # - C:\yoloDatasets\trainFinal\images 
  # - \\hpc-fs.qut.edu.au\turner30\Projects\Boats\Training\trainOrig\trainFinal\images

val: D:\Waterholes_project\counting_waterholes\deployment\train\val\images
  # - C:\ML_Software\MoretonVal\images
names:
  0: Dry_WH
  1: WH_swamp
  2: WH_wet
  3: WH_sink
  4: U

#If using manually the extract_zip() need to define the tif directory here: CHANGE:
tif_dir: D:\Waterholes_project\counting_waterholes\deployment\raw_images

#AF: various values and parameters of the model manipulation, to be checked when running it or change in the code directly. 
workers: 6
BATCH_SIZE: 8
EPOCHS: 500
img_size: 416
img_stride: 104
TILE_SIZE: 416
STRIDE: 104

#Testing configs. Added precision on the cutoff pixel stats:
CONFIDENCE_THRESHOLD: 0.7

#free to decide to change and figgle around with those values to cluster the detections together better. 
STAT_DISTANCE_CUTOFF_PIX_DRY: 120
STAT_DISTANCE_CUTOFF_PIX_WET: 120
STAT_DISTANCE_CUTOFF_PIX_SWAMP: 170
STAT_DISTANCE_CUTOFF_PIX_SINK: 120
STAT_DISTANCE_CUTOFF_PIX_U: 120

# The lat/long cutoff should be roughly related to the resolution of the images
# For 3m resolution, 10 pix is roughly 30m. At approx -27 deg lat, this is approx 0.0003 degrees.
STAT_DISTANCE_CUTOFF_LATLONG: 0.00025 
COMPARE_DISTANCE_CUTOFF_PIX: 40

#Tasks are used if you run the script via modules. I haven't ran all of this at all like that. 
#Tasks to be performed if running by modules. 
#I'm just putting all false and run manually in our pipeline. 
tasks:
  # These 4 are not required if have been done before (and above paths are absolute and exist)
  prepare: False # Prepare for labelling
  segment: False # Segment images/labels
  run_detection: False # Run the classifier
  backwards_annotation: False # Generate annotations from the classifier output

  analyse:
    # Main analysis step: cluster and compare outputs a comparison csv
    compare_detections_to_ground_truth: False # Cluster and compare results to ground truth (have to do this for all analysis below)

    # Supplementary analysis
    plots:
      confusion_matrix: False # Confusion matrix
      boat_count_compare: False # Compare boat counts in each image detections to ground truth and output a graph
    images:
      all_mistakes: False # Collate all mistakes into images with comparison
      subimage_confidence: False # Show confidence of detection for all subimages of a given boat (number of random boats to do this for)
      coverage_heatmap: False # generate a tif file with a heatmap of coverage of images in the test data
